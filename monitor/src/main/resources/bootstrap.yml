spring:
  application:
    name: monitor

server:
  port: 8771

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: true
    lease:
      duration: 5

turbine:
  appConfig: task-worker
  clusterNameExpression: "'default'"
